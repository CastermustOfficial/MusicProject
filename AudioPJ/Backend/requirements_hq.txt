# High-Quality Pipeline Requirements (HuggingFace Transformers)
# Install these in addition to base requirements if using PIPELINE_MODE="huggingface"

transformers>=4.36.0
accelerate>=0.25.0
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.4.0

# Audio processing
scipy>=1.11.0
numpy>=1.24.0

# For faster inference
optimum>=1.16.0
